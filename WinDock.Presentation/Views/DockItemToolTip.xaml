<Window x:Class="WinDock.Presentation.Views.DockItemToolTip"
                                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                                mc:Ignorable="d"
                                d:DesignHeight="300" d:DesignWidth="300"
                                Background="Transparent" 
                                SizeToContent="WidthAndHeight"
                                ShowInTaskbar="False"
        Topmost="True"
                                MinWidth="26"
        AllowsTransparency="True"
        WindowStyle="None"
                                BorderThickness="0">
    <Border Background="#99000000" Height="26" Padding="5" Margin="8" CornerRadius="13">
            <TextBlock Foreground="White" FontFamily="Lucida Sans Unicode" Text="{Binding Name}"></TextBlock>
        <Border.Effect>
            <DropShadowEffect Color="Black" ShadowDepth="0" />
        </Border.Effect>
    </Border>
</Window>
